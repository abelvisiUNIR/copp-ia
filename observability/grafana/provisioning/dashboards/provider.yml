apiVersion: 1
providers:
  - name: teleflow
    orgId: 1
    folder: TeleFlow
    type: file
    disableDeletion: false
    options:
      # No en /var/lib/grafana: ahí monta el volumen grafana-data y taparía la imagen
      # (ver grafana.Dockerfile).
      path: /etc/grafana/dashboards
