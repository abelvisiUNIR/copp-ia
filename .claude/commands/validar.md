---
description: Etapa 5 SDD — el subagente validador-spec valida contra spec.md y escribe validacion.md
argument-hint: <carpeta-feature>
---

Usa el subagente `validador-spec` para validar: $ARGUMENTS

Mostra el resultado de cada gate con conteos reales, el conteo CUMPLE / NO CUMPLE / SIN TEST y la
lista de lo que no cumple. Si hay NO CUMPLE o SIN TEST en una feature nueva, el siguiente paso es
`/implementar` con ese informe.
